# PostgreSQL Node

- PostgreSQL running on a dedicated node
- Listens on port 5432
- Access controlled via `pg_hba.conf`

## Notes
- SSH service must be enabled manually
- Firewall rules must allow port 5432
